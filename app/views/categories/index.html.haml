%p#notice= notice
%h1 Categories
%table
  %thead
    %tr
      %th Id
      %th Title
      %th Details
      %th Blocked at
      %th Blocker
      %th{:colspan => "3"}
  %tbody
    - @categories.each do |category|
      %tr
        %td= category.id
        %td= category.title
        %td= category.details
        %td= category.blocked_at
        %td= category.blocker_id
        %td= link_to 'Show', category
        %td= link_to 'Edit', edit_category_path(category)
        %td= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Category', new_category_path
