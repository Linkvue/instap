%p#notice= notice
%h1 Comments
%table
  %thead
    %tr
      %th Id
      %th Title
      %th Content
      %th Author
      %th Post
      %th Referer
      %th Published at
      %th Modified at
      %th Blocked at
      %th Blocker
      %th{:colspan => "3"}
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.id
        %td= comment.title
        %td= comment.content_id
        %td= comment.author_id
        %td= comment.post_id
        %td= comment.referer_id
        %td= comment.published_at
        %td= comment.modified_at
        %td= comment.blocked_at
        %td= comment.blocker_id
        %td= link_to 'Show', comment
        %td= link_to 'Edit', edit_comment_path(comment)
        %td= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Comment', new_comment_path
